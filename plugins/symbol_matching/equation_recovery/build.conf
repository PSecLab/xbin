# equation_recovery build inputs -- fill in YOUR local paths (per user).
# Read by build.sh. CLI args, if passed, override these. Leading ~ is expanded.
# Lines are "label: value"; anything left blank is prompted for at build time.

binja dir: "/scratch/Darpa/BIND/binaryninja"
license path: "/scratch/Darpa/BIND/license.dat"

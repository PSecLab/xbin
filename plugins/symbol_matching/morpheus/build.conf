# morpheus build inputs -- binja dir + license (per user). Used by build.sh; the
# binja/license are needed to (re)build the pysindy base it extends. CLI args
# override. Leading ~ is expanded.

binja dir: "/scratch/Darpa/BIND/binaryninja"
license path: "/scratch/Darpa/BIND/license.dat"

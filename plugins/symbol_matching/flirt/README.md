# `flirt_matcher` — IDA FLIRT signature matching

Matches functions against IDA FLIRT signature libraries and posts the recovered
symbol name to `symbol_matching`.

- **Base image:** `python:3.11-slim` (self-contained)
- **Weight:** `0.95`
